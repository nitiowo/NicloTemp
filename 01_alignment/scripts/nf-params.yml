# nf-core/rnaseq 3.21.0 parameter file
# Concatenated genomes: Btru + Shae genomes prefixed (Btru_/Shae_) and
# combined into a single FASTA/GTF. All reads map to combined genome simultaneously;
# count matrix is split by species prefix after the pipeline.
# BBsplit is disabled. Strandedness is reverse (confirmed from previous runs).
# Starting from raw lane-merged reads (trim.job merges raw lanes before this runs).

# ---- Input/output ----
# Update these paths to match your HPC directory structure
input: "NicloTemp/00_setup/samplesheet_raw.csv"
outdir: "NicloTemp/01_alignment/output/nfcore_output"
multiqc_title: "NicloTemp_concatenated_genome_full"

# ---- Reference genome ----
fasta: "NicloTemp/Data/reference_data/Combined/combined.genome.fna"
gtf: "NicloTemp/Data/reference_data/Combined/combined.genome.gtf"

# ---- Alignment ----
aligner: "star_salmon"
skip_bbsplit: true

# ---- Save intermediate files ----
save_reference: true
save_align_intermeds: true
save_unaligned: true

# ---- QC modules ----
rseqc_modules: "bam_stat,inner_distance,infer_experiment,junction_annotation,junction_saturation,read_distribution,read_duplication"
